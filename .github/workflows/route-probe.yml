name: Route time check (temporary)
on:
  push:
    branches: [claude/fuel-cost-calculator-kub0sf]
    paths: [scripts/route_probe.py, .github/workflows/route-probe.yml]
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/route_probe.py
